[pytest]
minversion = 7.0
addopts = -m "unit or integration or security"
testpaths = tests
markers =
    unit: fast, isolated unit tests
    integration: multi-module integration tests (no external network)
    security: security-focused tests (secrets, ignore rules)
